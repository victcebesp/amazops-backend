handler: ./src/handler/stripeWebhook.handler
role: StripeWebhookRole
events:
  - httpApi:
      path: /webhook
      method: post
environment:
  STRIPE_KEY: ${ssm:STRIPE_KEY-${self:provider.stage}}
  STRIPE_WEBHOOK_SECRET: ${ssm:STRIPE_WEBHOOK_SECRET-${self:provider.stage}}