handler: ./src/handler/querySubscription.handler
role: QuerySubscriptionRole
events:
  - httpApi:
      path: /subscription
      method: get
      authorizer:
        name: jwtAuthorizer
environment:
  STRIPE_KEY: ${ssm:STRIPE_KEY-${self:provider.stage}}