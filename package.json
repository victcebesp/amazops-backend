{
  "name": "amazops-backend",
  "version": "1.0.0",
  "description": "This is the backend of Amazops.app, a saas for amazon fba sellers to handle all the business operations",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "sls deploy --aws-profile amazops --stage prod",
    "deploy-dev": "sls deploy --aws-profile amazops --stage dev",
    "deploy-test": "sls deploy --aws-profile amazops --stage test",
    "lint": "eslint .  --ext .js",
    "lint-fix": "eslint --fix .  --ext .js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.294.0",
    "@aws-sdk/lib-dynamodb": "^3.294.0",
    "jsonwebtoken": "^9.0.0",
    "moment": "^2.29.4",
    "stripe": "^12.0.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "eslint": "^8.36.0",
    "eslint-config-standard": "^17.0.0",
    "eslint-plugin-import": "^2.27.5",
    "eslint-plugin-n": "^15.6.1",
    "eslint-plugin-promise": "^6.1.1"
  }
}
